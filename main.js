// Autohaus Rheinblick – Coming Soon
// Fade-in beim Laden bereits via CSS-Animation geregelt.
// Dieses Script reserviert Platz für spätere Erweiterungen.

document.addEventListener('DOMContentLoaded', function () {
  // E-Mail-Link: Klick-Tracking (optional erweiterbar)
  var emailLink = document.querySelector('.email');
  if (emailLink) {
    emailLink.addEventListener('click', function () {
      // Hier können später Analytics-Events ergänzt werden
    });
  }
});
